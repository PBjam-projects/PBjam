[pytest]
markers =
    integration: tests that run external sampling algorithms or full workflows
    packaging: tests that build or install the project
    docs: tests that invoke the Sphinx documentation toolchain
